# Base URL (including /api) used when the client is deployed separately from the API.
# Leave unset for local development: Vite proxies /api to VITE_API_TARGET.
VITE_API_URL=

# Local Express API in ../server
VITE_API_TARGET=http://localhost:8081
